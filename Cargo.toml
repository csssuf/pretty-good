[package]
name = "pretty-good"
version = "0.1.0"
authors = ["James Forcier <james.forcier@coreos.com>"]

[dependencies]
byteorder = "1.2"
failure = "0.1"
failure_derive = "0.1"
gcrypt = "0.5"

[dependencies.nom]
version = "^3.2"
features = ["verbose-errors"]
